python -m scripts.download_and_build --sources data\raw\arxiv-metadata-oai-snapshot.json --db_path data\arxiv.sqlite3 --raw_dir data\raw --processed_dir data\processed --max_papers 10000


# 1. Chuẩn bị dữ liệu
python preprocess_data.py  --dataset scidocs --input_dir "../data/raw/scidocs" --output_dir "../data/scidocs" --validation_ratio 0.1 --test_ratio 0.1

# Scidocs hard negatives (2 hard + 2 random)
python preprocess_data_hard_neg.py --dataset scidocs --input_dir "../data/raw/scidocs" --output_dir "../data/scidocs_hard"

# SciFact với toàn hard negatives
python preprocess_data_hard_neg.py \
    --dataset scifact \
    --input_dir ../data/raw/scifact \
    --output_dir ../data/scifact_allhard \
    --negatives_per_positive 4 \
    --n_hard 4


# 2. Train (chỉ cần sửa YAML, không cần đụng code)
python train.py --config configs/reranking_scifact.yaml

# 3. Override tham số từ CLI
python train.py --config configs/reranking_scifact.yaml --num_epochs 5 --learning_rate 1e-5

# 4. Đánh giá checkpoint độc lập
python evaluate.py --task document_reranking \
    --checkpoint outputs/reranking_scifact/best_checkpoint \
    --data_file ../data/reranking/test.jsonl


cd app\backend
python -m scripts.build_db --input_file data\raw\arxiv_snapshot.json --db_path data\papers.sqlite3 --processed_dir data\processed --max_papers 50000